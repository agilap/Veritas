- transformers>=4.40.0
- torch>=2.2.0
- open-clip-torch>=2.24.0
- opencv-python>=4.9.0
- gradio>=4.0.0
- requests>=2.31.0
- beautifulsoup4>=4.12.0
- datasets>=2.18.0
- accelerate>=0.29.0
- peft>=0.10.0
- python-dotenv>=1.0.0
- pillow>=10.0.0
- tavily-python>=0.3.0
- scikit-learn>=1.4.0

# optional (requires ~6GB VRAM): pip install salesforce-lavis

bitsandbytes>=0.43.0
# optional captioning model (lightweight, ~990MB): already included via transformers
# BLIP-2 (blip2-opt-2.7b) is NOT used — too large for 4GB VRAM
# Use Salesforce/blip-image-captioning-base instead (loaded via transformers)
